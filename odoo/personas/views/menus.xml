<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>

    <!-- Menú raíz -->
    <menuitem id="personas_menu_root" name="Personas" sequence="20"/>

    <!-- Personas (base) -->
    <record id="personas_action_personas" model="ir.actions.act_window">
      <field name="name">Personas</field>
      <field name="res_model">persona.persona</field>
      <field name="view_mode">list,form</field>
    </record>

    <menuitem id="personas_menu_personas"
              name="Todas las personas"
              parent="personas_menu_root"
              action="personas_action_personas"
              sequence="10"/>

    <!-- Clientes -->
    <record id="personas_action_clientes" model="ir.actions.act_window">
      <field name="name">Clientes</field>
      <field name="res_model">clientes.cliente</field>
      <field name="view_mode">list,form</field>
    </record>

    <menuitem id="personas_menu_clientes"
              name="Clientes"
              parent="personas_menu_root"
              action="personas_action_clientes"
              sequence="20"/>

    <!-- Proveedores -->
    <record id="personas_action_proveedores" model="ir.actions.act_window">
      <field name="name">Proveedores</field>
      <field name="res_model">persona.proveedor</field>
      <field name="view_mode">list,form</field>
    </record>

    <menuitem id="personas_menu_proveedores"
              name="Proveedores"
              parent="personas_menu_root"
              action="personas_action_proveedores"
              sequence="30"/>

    <!-- Empleados
    <record id="personas_action_empleados" model="ir.actions.act_window">
      <field name="name">Empleados</field>
      <field name="res_model">persona.empleado</field>
      <field name="view_mode">list,form</field>
    </record>

    <menuitem id="personas_menu_empleados"
              name="Empleados"
              parent="personas_menu_root"
              action="personas_action_empleados"
              sequence="40"/-->

  </data>
</odoo>
